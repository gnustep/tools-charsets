NSCharacterSet Utilities
************************
   Utilities for use with NSCharacterSet bitmap files.

   mkcharsets: Creates the standard set of bitmap files for NSCharacterSet,
in the current directory.  Takes no argument.

   ckcharset: Takes filename of character set bitmap as argument and prints
out the Unicode names of all of the characters in the character set.
